{
    "name": "pdf-generator",
    "version": "1.0.0",
    "description": "Lambda function for generating PDFs",
    "main": "index.js",
    "scripts": {
        "deploy": "npm install && zip -r function.zip . -x '*.git*' -x '*.log' -x 'node_modules/*' -x '.lambdaignore'"
    },
    "dependencies": {
        "@aws-sdk/client-lambda": "^3.632.0",
        "chrome-aws-lambda": "^10.1.0",
        "dotenv": "^16.4.5",
        "puppeteer-core": "^23.1.0",
        "tmp": "^0.2.3"
    }
}