{
    "name": "pdf-generator",
    "version": "1.0.0",
    "description": "Lambda function for generating PDFs",
    "main": "index.js",
    "scripts": {
        "postinstall": "npx playwright install",
        "deploy": "npm install && zip -r function.zip . -x '*.git*' -x '*.log' -x '.lambdaignore'"
    },
    "dependencies": {
        "@aws-sdk/client-lambda": "^3.632.0",
        "playwright": "^1.46.1",
        "tmp": "^0.2.3"
    }
}
